name: Trigger auto deployment for ca-api-dq5tefek4ngfc

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-api-dq5tefek4ngfc-AutoDeployTrigger-55b99f06-8d8b-4f02-ada8-bdf0394e4f26.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CAAPIDQ5TEFEK4NGFC_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAAPIDQ5TEFEK4NGFC_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAAPIDQ5TEFEK4NGFC_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}https://github.com/ThinThin-15/AIAgentsTest/blob/main/src
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: testagent93.azurecr.io
          registryUsername: ${{ secrets.CAAPIDQ5TEFEK4NGFC_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAAPIDQ5TEFEK4NGFC_REGISTRY_PASSWORD }}
          containerAppName: ca-api-dq5tefek4ngfc
          resourceGroup: rg-Myagent
          imageToBuild: testagent93.azurecr.io/ca-api-dq5tefek4ngfc:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


